ollama:
    candy:
        version: 2026.229.1218
        description: |
            GPU-agnostic Ollama LLM inference server on port 11434
            Installs the Ollama binary and runs `ollama serve` as a custom supervisord
            service that listens on 0.0.0.0:11434 (no systemd). Two install paths, one
            resulting layout (/usr/bin/ollama plus the backend libraries under
            /usr/lib/ollama): on a distro that PACKAGES ollama the package is installed
            and the package manager owns the version; everywhere else the upstream
            release tarball is extracted to /usr at the version the var OLLAMA_VERSION
            selects. That var is PINNED to a release tag and must stay one: the download
            cache is content-addressed by the sha256 of the URL, so a `latest` URL would
            hash to one key forever and serve the first tarball it ever fetched, long
            after upstream moved on. The candy declares NO GPU dependency — a GPU backend is
            an IMAGE-level composition choice (compose ollama-cuda or ollama-rocm). What that
            buys differs by install path, and the difference is worth stating rather than
            averaging: on a PACKAGED distro the base package is genuinely CPU-only at 66 MiB
            and a backend is strictly opt-in, which is what keeps this layer small; on the
            TARBALL path the single upstream archive already contains the CUDA backend, so a
            tarball consumer pays for it whether or not any box composes ollama-cuda. Pulled models persist under the ~/.ollama models volume. The running
            service exposes the Ollama HTTP API (e.g. /api/tags) so any composing box
            can serve and query local models. The companion plugin candy plugin-ollama
            adds the `charly ollama` management CLI against a deployed server.
        require:
            # GPU-agnostic: the ollama binary auto-detects the GPU at runtime (CPU
            # fallback when none present), so CUDA is an IMAGE-level composition choice
            # (the `ollama` image lists `cuda` explicitly), NOT a layer dependency.
            # This lets CPU consumers (e.g. `openclaw-desktop` on cachyos) compose the
            # layer without dragging the Fedora/NVIDIA `cuda` layer onto a pac base.
            - '@github.com/opencharly/layer-supervisord:v2026.235.2056'
        var:
            # Upstream Ollama release for the TARBALL path only — the distros that
            # package ollama let their package manager own the version.
            #
            # A concrete tag, NOT `latest`, and the reason is correctness rather than
            # taste. The download verb content-addresses its cache by sha256 OF THE URL.
            # With `latest` the URL is constant while the artifact behind it changes, so
            # the cache would serve whichever build was fetched first, forever — a silent
            # pin to an arbitrary old version that no amount of rebuilding would shake
            # loose. A `latest` URL and a content-addressed cache are incompatible; the
            # tag is what makes the key honest.
            #
            # (Secondarily: GitHub serves the two cases from structurally different URLs —
            # releases/latest/download/... vs releases/download/<tag>/... — so one template
            # could not express both anyway, and pinning matches the house pattern set by
            # CUE_VERSION and APKEEP_VERSION.) Bump it here to move the tarball distros.
            OLLAMA_VERSION: v0.32.14
        distro:
            # Arch (and its derivatives, CachyOS included) ships ollama in `extra`,
            # tracking upstream closely — 0.32.14-1 landed the day upstream tagged
            # v0.32.14. The packaged base is 66 MiB installed (16 MiB download); the
            # upstream tarball it replaces is a 1.32 GiB DOWNLOAD, because that one
            # archive bundles the CPU and CUDA backends together (ROCm is NOT in it —
            # upstream publishes that as a separate ollama-linux-amd64-rocm archive),
            # while the packages split even CUDA out into the ollama-cuda add-on this
            # candy deliberately does NOT require.
            arch:
                package:
                    - ollama
        env:
            OLLAMA_HOST: "0.0.0.0"
            OLLAMA_MODELS: "~/.ollama/models"
        env_provide:
            OLLAMA_HOST: "http://{{.ContainerName}}:11434"
        port:
            - 11434
        volume:
            - name: models
              path: "~/.ollama"
        alias:
            - name: ollama
              command: ollama
        service:
            - name: ollama
              exec: "ollama serve"
              restart: always
              enable: true
              scope: system
        plan:
            # Capability-gated, not distro-gated. The `distro:` package sections above
            # compile into the plan BEFORE this step (BuildDeployPlan phase 1 vs phase 3),
            # so on a distro that packages ollama the binary already exists here and
            # `unless_exists:` skips the fetch entirely. Detection rather than a distro
            # name, so any distro that later gains an ollama package is picked up with no
            # edit — and a plan step has no build-time distro filter to gate it with
            # anyway (`exclude_distro:` is read by the check runner, not the emitter).
            #
            # This is the declarative `download:` verb rather than a hand-rolled curl, so
            # it keeps what the verb provides and a hand-rolled step forfeits: the
            # content-addressed /tmp/downloads cache (no re-fetch of a 1.32 GiB archive on
            # every cache-miss build), the atomic .part -> mv rename, and the stage-user
            # -correct cache mount. `unless_exists:` is the capability that made that
            # possible; before it, the gate could only be expressed by abandoning the verb.
            - run: install the upstream ollama release tarball on distros that ship no ollama package (var OLLAMA_VERSION)
              download: "https://github.com/ollama/ollama/releases/download/${OLLAMA_VERSION}/ollama-linux-amd64.tar.zst"
              extract: tar.zst
              to: /usr
              unless_exists: /usr/bin/ollama
              run_as: root
            - check: the ollama binary is installed at /usr/bin/ollama
              file:
                file: /usr/bin/ollama
                exists: true
            - check: the running ollama service answers the model-tags API with HTTP 200 on port 11434
              context:
                - runtime
              http:
                http: http://127.0.0.1:${HOST_PORT:11434}/api/tags
                status: 200
            - check: the ollama CLI reports an "ollama version" string
              stdout:
                - matches: "^ollama version"
              context:
                - runtime
              command: ollama --version
            - check: the ollama CLI lists models against the live service with exit 0
              exit_status: 0
              context:
                - runtime
              command: ollama list
            # Host-side proof of the companion command:ollama plugin, authored HERE on
            # the candy that provides the server rather than on any one bed, so every
            # box composing ollama inherits it (R3) instead of each bed repeating the
            # steps. ${CHARLY_BIN} is the ACTIVE charly (kit.StampCharlyBin), never a
            # stale PATH pick, and is stamped for any runtime check run — not just beds.
            - check: charly ollama help exits 0 and prints the plugin usage, proving the compiled-in command:ollama plugin registers and dispatches
              id: ollama-cli-help
              exit_status: 0
              stdout:
                - contains: 'manage a deployed Ollama server'
              context:
                - runtime
              command:
                command: ${CHARLY_BIN} ollama help
                in_container: false
            - check: charly ollama version reports the server version over the published port, proving a live API round-trip against the deployed server
              id: ollama-cli-version
              exit_status: 0
              stdout:
                - contains: 'ollama server version is'
              context:
                - runtime
              command:
                command: ${CHARLY_BIN} ollama version --server http://127.0.0.1:${HOST_PORT:11434}
                in_container: false
            - check: charly ollama list prints the model table header over the published port, proving the list API path against the deployed server
              id: ollama-cli-list
              exit_status: 0
              stdout:
                - contains: 'NAME'
              context:
                - runtime
              command:
                command: ${CHARLY_BIN} ollama list --server http://127.0.0.1:${HOST_PORT:11434}
                in_container: false
            - agent-check: models pulled via `ollama pull` are stored under the ~/.ollama models volume and survive a service restart

ollama-skill:
    skill:
        name: ollama
        family: ollama
        owner: ollama
        description: |-
            Standalone Ollama LLM inference server. This candy DECLARES no GPU dependency —
            a backend is composed at the box level (`ollama-cuda` / `ollama-rocm`). What that
            yields differs by install path: on a packaged distro the base is genuinely CPU-only,
            while on a tarball distro the single upstream archive already contains CUDA, so the
            declaration is about what this candy REQUIRES, not about what ends up in the image.
            Runs as a supervisord service on port 11434 with persistent model storage.
            MUST be invoked before building, deploying, configuring, or troubleshooting the ollama box.
        content: |-
            # ollama
            
            Ollama LLM inference server. On a distro that packages ollama it is CPU-only by
            itself and a GPU backend is composed at the box level; on a tarball distro the
            upstream archive already bundles the CUDA backend.
            
            ## Box Properties
            
            The `ollama` BOX lives in a distro submodule — `box/fedora` and `box/cachyos` each
            define one today — not here, so its exact
            composition is stated there and not on this page — an earlier version of this
            table listed a candy set that a later box edit made wrong, which is the failure
            mode a cross-repo composition table has by construction. What is stable, and
            all this page should claim:
            
            | Property | Value |
            |----------|-------|
            | Ports | 11434 |
            | Volumes | `models` → `~/.ollama` |
            | Registry | ghcr.io/opencharly |
            
            The GPU base and the backend candy it composes are the box's choice; see
            `charly box list candies ollama` for what a given box actually stacks.
            
            ## GPU is box-level — the `ollama` candy is GPU-agnostic
            
            The `ollama` **candy** installs the Ollama binary and a supervisord
            `ollama serve` service, and carries **no GPU dependency** — it only
            `require:`s `supervisord`. Two install paths converge on one layout
            (`/usr/bin/ollama` plus backends under `/usr/lib/ollama`): on Arch and its
            derivatives the packaged `ollama` (66 MiB installed) is installed, and elsewhere
            the upstream release tarball is extracted to `/usr`. The step is gated on
            whether the binary already exists rather than on a distro name, so any distro
            that gains an ollama package is picked up without an edit.

            GPU support is a **composition choice made at the box level** — but only on the
            PACKAGED path is it also a size choice, and conflating the two is easy to do:

            - **Packaged distros** (Arch and derivatives) install a 66 MiB CPU-only base, and a
              backend is strictly opt-in. This is where the split earns its keep.
            - **Tarball distros** get one archive (1.32 GiB compressed) that already contains
              the CUDA backend, so there is nothing to opt out of. ROCm is the exception in the
              other direction: it is NOT in that archive — upstream publishes it as a separate
              `ollama-linux-amd64-rocm` download, which this candy does not select. That is why
              `ollama-rocm` is packaged for Arch only — it has no candy page of its own to say so
              on, since it declares no `skill:` entity, so the constraint is stated here.

            Every size on this page is INSTALLED except ONE: the 1.32 GiB tarball figure, which
            is a COMPRESSED DOWNLOAD. (The 66 MiB figures above are installed sizes, so the axis
            does not split by position on the page — it splits by which figure.) The download is
            deliberately not compared against the installed numbers: the tarball's installed size
            is not stated anywhere here, so weighing 1.32 GiB against them would compare two
            different axes. Read it as what a tarball build FETCHES. The two
            figures in the bullets below are the two ALTERNATIVE BACKENDS, not the cost of a
            packaged build: a packaged image pays the 66 MiB base plus AT MOST ONE of them, and
            pays neither if it composes neither. Both are Arch figures — off Arch `ollama-cuda`
            adds no package at all, so its ~988 MiB is not a cost a tarball image pays on top.
            (They are bullets, not a table — this page's only tables are Box Properties, Ports
            and Volumes.)

            - A GPU box composes `ollama-cuda` (~988 MiB) beside this candy for the
              CUDA backend, or `ollama-rocm` (~2.9 GiB) for AMD. Both are candies in this
              repository and BOTH package only on Arch (`distro: {arch: …}` and nothing
              else), which means the two behave differently off it: `ollama-cuda` degrades
              into an ASSERTION that the tarball's bundled CUDA is present, while
              `ollama-rocm` has no tarball path at all and its check correctly fails rather
              than pretending. A ROCm image is therefore an Arch-family image. Which boxes
              compose which backend is decided in the distro submodules and changes there,
              so consult those for the current list rather than this page — and read any
              such list through the Arch-only rule above, which is the part that does not
              move.
            - A consumer that composes neither pays for neither ON A PACKAGED DISTRO —
              e.g. `/charly-openclaw:openclaw-desktop` (cachyos base, no GPU candy). On a
              tarball distro it still carries the bundled CUDA backend.
            
            ## Ports
            
            | Port | Service | Protocol |
            |------|---------|----------|
            | 11434 | Ollama API | HTTP |
            
            ## Volumes
            
            | Name | Path | Purpose |
            |------|------|---------|
            | models | ~/.ollama | Model storage |
            
            ## Quick Start
            
            ```bash
            charly box build ollama
            charly config ollama
            charly start ollama
            charly shell ollama -c "ollama pull llama3"
            charly shell ollama -c "ollama run llama3 'Hello'"
            ```
            
            ## Host Alias
            
            ```bash
            charly alias install ollama
            # Now: ollama pull llama3  (runs inside the container)
            ```
            
            ## The `charly ollama` management CLI
            
            The compiled-in `command:ollama` plugin (`candy/plugin-ollama`) manages a
            deployed Ollama server from the host over its HTTP API — no shell alias
            needed:
            
            **11434 is the port INSIDE the container, not necessarily on your host.** A deploy
            can publish it elsewhere, and a check bed allocates the host port automatically. Read
            it from the `Ports:` line before you connect:

            ```bash
            charly status ollama
            #   Ports:     34117:11434/tcp        <- HOST:CONTAINER, so the host port is 34117
            charly ollama list                --server http://127.0.0.1:34117
            charly ollama pull llama3         --server http://127.0.0.1:34117
            charly ollama run  llama3 "Hello" --server http://127.0.0.1:34117
            ```

            On a deploy that publishes 11434 unchanged, drop the flag entirely — that is the
            default. On a bed-allocated port you cannot, and a copied hardcoded 11434 gets you a
            connection refused.

            Endpoint resolution: `--server` flag > `OLLAMA_HOST` env > `http://127.0.0.1:11434`.
            See `/charly-ollama:ollama-cli` for the full command tree.
            
            ## Pinning the upstream version
            
            `OLLAMA_VERSION` is PINNED to a release tag (`v0.32.14` today) and must
            stay one — this is not a default you can swap for `latest`. The download
            cache is content-addressed by the sha256 of the URL, so a `latest` URL
            hashes to a single key forever and keeps serving the first tarball it ever
            fetched, long after upstream has moved on. Bump the tag to move the tarball
            distros; the packaged distros ignore it entirely, because pacman owns the
            version there.
            
            ## Service Environment
            
            When deployed via `charly config ollama`, this box automatically provides `OLLAMA_HOST=http://charly-ollama:11434` to all other deployed containers via the `env_provide` mechanism. Use `--update-all` to propagate to already-deployed services:
            
            ```bash
            charly config ollama --update-all
            ```
            
            This means containers like `jupyter-ml-notebook` automatically discover the Ollama endpoint without manual `OLLAMA_HOST` configuration.
            
            ## Related Candies
            
            - `/charly-ollama:ollama-layer` — the ollama CANDY: the binary, the supervisord
              service and the models volume. This page is the BOX; that page is what it composes.
            - `/charly-ollama:ollama-cli` — the compiled-in `charly ollama` management CLI (candy/plugin-ollama)
            - `ollama-cuda` / `ollama-rocm` — the opt-in GPU backend candies. Named in plain
              backticks, not as `/charly-<plugin>:<skill>` references: neither candy declares a
              `skill:` entity, so a reference form would be a well-formed link to nothing and the
              docs generator's fail-closed gate rejects it.
            - `/charly-jupyter:notebook-ollama` — 6 Jupyter notebooks demonstrating Ollama APIs (requests, OpenAI, ollama lib, Anthropic, HuggingFace, GPU)
            
            ## Related Boxes
            
            Deliberately NOT a list of what composes what. Every such statement here is a claim
            about a `box/<distro>` submodule that this page cannot keep true — the composition
            table above this section used to make exactly that claim and went stale. These are
            pointers to related SUBJECTS; run `charly box list candies <box>` for any actual
            composition.
            
            - `/charly-distros:nvidia` — the GPU base family
            - `/charly-distros:cachyos` — the CachyOS image family, which carries its own ollama boxes
            - `/charly-openclaw:openclaw-desktop` — a desktop image that serves a local ollama
            - `/charly-jupyter:jupyter-ml-notebook` — receives `OLLAMA_HOST` via env_provide when ollama is deployed
            - `/charly-openwebui:openwebui` — receives `OLLAMA_HOST` via env_provide, auto-configures as `OLLAMA_BASE_URL`
            
            ## Verification
            
            After `charly start`:
            - `charly status ollama` — container running
            - `charly service status ollama` — all services RUNNING
            - `curl -s http://localhost:<host-port>/api/tags` — Ollama API responds, where
              `<host-port>` is the left-hand side of the `Ports:` line in `charly status ollama`
              (that line reads HOST:CONTAINER/proto; 11434 is the container-side port)
            - `charly check live ollama` — the baked runtime checks pass: `/api/tags` 200,
              `ollama --version`, `ollama list` exit 0. This is the only one of these that
              reports on the checks; `charly ollama list` lists MODELS and says nothing about
              whether anything passed.
            
            ## When to Use This Skill
            
            **MUST be invoked** when the task involves the ollama box, LLM model serving, or the standalone Ollama deployment. Invoke this skill BEFORE reading source code or launching Explore agents.
            
            ## Related
            
            - `/charly-image:image` — image family umbrella (`candy:` image entries — those carrying `base:`/`from:` — in `charly.yml`, build/validate/inspect/list)
            - `/charly-build:build` — the embedded build vocabulary (distros, builders, init-systems)
